# 🚀 Как залить LinkPage на хостинг

## Вариант 1: Vercel (самый простой, рекомендую)

### Шаг 1 — Установи Node.js
Скачай и установи: https://nodejs.org (жми LTS версию)

### Шаг 2 — Разархивируй проект
Разархивируй `linkpage-app.zip` в любую папку

### Шаг 3 — Установи зависимости
Открой терминал (или cmd/PowerShell) в папке проекта и выполни:
```
npm install
```

### Шаг 4 — Проверь локально (по желанию)
```
npm run dev
```
Откроется в браузере по адресу http://localhost:5173

### Шаг 5 — Загрузи на GitHub
1. Зайди на https://github.com и создай аккаунт (если нет)
2. Нажми **New repository** → назови `linkpage` → Create
3. В терминале в папке проекта:
```
git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/ТВОЙ_ЛОГИН/linkpage.git
git push -u origin main
```

### Шаг 6 — Деплой на Vercel
1. Зайди на https://vercel.com и войди через GitHub
2. Нажми **Add New → Project**
3. Выбери репозиторий `linkpage`
4. Жми **Deploy**
5. Через ~1 минуту получишь ссылку вида `linkpage-xxx.vercel.app`

### Шаг 7 — Свой домен (по желанию)
В настройках проекта на Vercel → Domains → добавь свой домен

---

## Вариант 2: Netlify (drag & drop)

### Шаги 1-3 — как выше

### Шаг 4 — Собери проект
```
npm run build
```
Появится папка `dist`

### Шаг 5 — Залей на Netlify
1. Зайди на https://app.netlify.com/drop
2. Перетащи папку `dist` прямо в браузер
3. Готово! Получишь ссылку

---

## Вариант 3: GitHub Pages (полностью бесплатно)

### Шаги 1-3 — как в Варианте 1

### Шаг 4 — Установи gh-pages
```
npm install --save-dev gh-pages
```

### Шаг 5 — Добавь в package.json:
В раздел `"scripts"` добавь:
```
"deploy": "vite build && gh-pages -d dist"
```

А в корень package.json добавь:
```
"homepage": "https://ТВОЙ_ЛОГИН.github.io/linkpage"
```

### Шаг 6 — Деплой
```
npm run deploy
```

---

## 💡 Полезные команды

| Команда | Что делает |
|---------|-----------|
| `npm run dev` | Запуск локально для разработки |
| `npm run build` | Сборка для продакшена (папка dist) |
| `npm run preview` | Предпросмотр собранной версии |

## ❓ Проблемы?

- **"npm не найден"** → Установи Node.js и перезапусти терминал
- **"git не найден"** → Установи Git: https://git-scm.com
- **Белый экран после деплоя** → В `vite.config.js` добавь `base: './'`
